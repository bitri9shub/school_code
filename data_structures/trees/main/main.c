/*
    Binary search tree
*/

#include "headers.h"

int main()
{
    node* leaf;

    // Initialize a tree

    initialize_node(&leaf, 15);
    // printf("%d %p %p", leaf->value, leaf->right_child, leaf->left_child);

    // Insert element to a tree
    // Search for an element in a tree
    // Remove element in a tree
    return 0;
}
